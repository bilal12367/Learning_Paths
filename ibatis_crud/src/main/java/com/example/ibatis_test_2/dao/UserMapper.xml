<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- <mapper namespace="com.example.ibatis_tutorial.dao.UserMapper"> -->
<mapper namespace="com.example.ibatis_tutorial3.dao.UserMapper">

    <resultMap id="result" type="user">
        <id property="id" column="id" />
        <result property="username" column="username"/>
        <result property="email" column="email"/>
        <result property="address" column="address"/>
    </resultMap>

    <select id="getAllUsers" resultType="user" resultMap="result">

        SELECT * FROM USERS
    </select>

    <insert id="addUser" parameterType="user">
        INSERT INTO USERS(id,username,email,address) VALUES(#{id},#{username},#{email},#{address});
    </insert>
<!--     
    <select id="getUserById" parameterType="java.lang.Integer" resultType="com.example.mybatis.models.User">
        SELECT * FROM users WHERE id = #{id}
    </select>

    
    <insert id="insertUser" parameterType="com.example.mybatis.models.User">
        INSERT INTO users (username, password) VALUES (#{username}, #{password})
    </insert>

    
    <update id="updateUser" parameterType="com.example.mybatis.models.User">
        UPDATE users SET username = #{username}, password = #{password} WHERE id = #{id}
    </update>

    
    <delete id="deleteUser" parameterType="java.lang.Integer">
        DELETE FROM users WHERE id = #{id}
    </delete> -->

</mapper>
